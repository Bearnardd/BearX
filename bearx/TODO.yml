TODO:
    - assert shape in sigmoid for example
    - think about converting activation functions to layers
    - add docs
    - add regulizers
    - change model to use __call__??
    - add Initializers to Linear layer
    - add shape checking
    - add validating embedding layer to be only first
    - add gates to backend??
    - add RNN(Layer) so all rnn's layers can inherit
